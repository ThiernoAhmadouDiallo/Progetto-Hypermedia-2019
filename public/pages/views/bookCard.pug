each book in bookList
    .card(style='width: 18rem;')
    img.card-img-top(src='...', alt='...')
    .card-body
        h5.card-title book.title
        p.card-text
            book.price
        a.btn.btn-primary(href='#') Rent Book